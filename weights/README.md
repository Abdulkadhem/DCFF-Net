# Checkpoints

Trained weights are not stored in git. Download them from the
[Releases](../../releases) page into this directory.

See the checkpoint table in the top-level README for which file
reproduces which result.
